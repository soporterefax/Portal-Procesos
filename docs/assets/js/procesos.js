let currentUser=null, cache=[], areas=[];
function closeForm(){modal.hidden=true;}
function fillAreas(){const opts=areas.map(a=>`<option value="${a.id}">${esc(a.nombre)}</option>`).join("");area_id.innerHTML='<option value="">Seleccione...</option>'+opts;areaFilter.innerHTML='<option value="">Todas las áreas</option>'+opts;}
function openForm(p=null){id.value=p?.id||"";codigo.value=p?.codigo||"";area_id.value=p?.area_id||"";nombre.value=p?.nombre||"";tipo.value=p?.tipo||"";estado.value=p?.estado||"Activo";responsable.value=p?.responsable||"";persona_responsable.value=p?.persona_responsable||"";objetivo.value=p?.objetivo||"";areas_relacionadas.value=p?.areas_relacionadas||"";fecha_actualizacion.value=p?.fecha_actualizacion||"";es_critico.checked=!!p?.es_critico;formTitle.textContent=p?"Editar proceso":"Nuevo proceso";modal.hidden=false;}
async function meta(){const d=await apiGet('/api/procesos/meta');areas=d.areas||[];fillAreas();}
async function load(){setLoading('rows',4,'Cargando procesos...');try{const p=new URLSearchParams();if(q.value)p.set('q',q.value);if(areaFilter.value)p.set('area_id',areaFilter.value);if(estadoFilter.value)p.set('estado',estadoFilter.value);const d=await apiGet(`/api/procesos?${p}`);cache=d.procesos||[];rows.innerHTML=cache.length?cache.map(x=>`<tr><td><div class="cell-title"><strong>${esc(x.nombre)}</strong><span class="cell-sub">${esc(x.codigo)}</span></div></td><td>${esc(x.area?.nombre||'-')}</td><td>${x.estado==='Inactivo'?'<span class="status-inactive">Inactivo</span>':'<span class="status-active">Activo</span>'}</td>${currentUser.rol==='administrador'?`<td class="admin-only-column"><div class="action-row"><button class="btn btn-secondary btn-sm" onclick="openForm(cache.find(y=>y.id===${x.id}))">Editar</button><button class="btn btn-danger btn-sm" onclick="removeP(${x.id})">Eliminar</button></div></td>`:''}</tr>`).join(''):`<tr><td colspan="${currentUser?.rol==='administrador'?4:3}" class="empty">No hay procesos para mostrar.</td></tr>`;}catch(e){showMsg(e.message,'error');}}
async function removeP(pid){if(!confirm('¿Eliminar este proceso?'))return;try{await apiDelete(`/api/procesos/${pid}`);showMsg('Proceso eliminado');await load();}catch(e){showMsg(e.message,'error');}}
form.addEventListener('submit',async e=>{e.preventDefault();const body={codigo:codigo.value,area_id:area_id.value,nombre:nombre.value,tipo:tipo.value,estado:estado.value,responsable:responsable.value,persona_responsable:persona_responsable.value,objetivo:objetivo.value,es_critico:es_critico.checked,areas_relacionadas:areas_relacionadas.value,fecha_actualizacion:fecha_actualizacion.value};try{id.value?await apiPut(`/api/procesos/${id.value}`,body):await apiPost('/api/procesos',body);closeForm();showMsg('Proceso guardado');await load();}catch(e){showMsg(e.message,'error');}});
(async()=>{currentUser=await initLayout('procesos');if(!currentUser)return;await Promise.all([meta(),load()]);})();
